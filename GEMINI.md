# 🧬 GEMINI COMMAND: GENESISAI PROTOCOL

> **DEPRECATION WARNING:** The register-based VM architecture (`KrakoaRunner.ts`) is legacy. Do NOT attempt to expand or debug it unless explicitly asked for archaeological purposes.

## 🏛️ THE NEW PHILOSOPHY: IDENTITY OVER EXECUTION

As the AI Agent responsible for this codebase, you must adhere to the **Identity Matrix & Behavior Tree** architecture. We are no longer building a computer; we are building a **Deterministic Identity Engine**.

### 1. The Core Objective
The goal of GenesisAI is to transform **Krakoa Nexus DSL (.ksl)** into a dense, structured **JSON Identity Matrix**. This matrix is injected into LLMs to prevent "Contextual Drift" and ensure "Semantical Determinism".

### 2. Implementation Mandates (Transpiler-First)
- **Flattening over Recursion:** Use the `Peggy.js` AST to generate a flat list of commands or a shallow JSON structure. Avoid deep recursive execution.
- **Tcl-Style Commands:** Treat every DSL token (`👤`, `🧬`, `⚓`) as a discrete command that modifies the Identity Matrix.
- **Behavior Tree Logic:** Implement `⚓` (Anchors) as **Guards**. If an anchor's lambda returns false, the entire branch in the JSON should be omitted or marked as inactive.
- **JSON Density:** Every byte of the output JSON must serve a purpose for the LLM's context window. Avoid verbose keys.

### 3. Coding Standards
- **Strict Typing:** No `any`. Use the structured types defined in the AST.
- **Immutability:** The original AST must never be modified. The Transpiler should produce a new, clean JSON object.
- **Ollama Integration:** Ensure the generated JSON is compatible with local inference constraints (focus on clarity and key-value anchoring).

### 4. Strategic Direction
Refer to `07_THE_LABYRINTH/GEN-0141` and `GEN-0142` in the main Vault for deep architectural reasoning. This `GEMINI.md` is your tactical field manual.

---
**DIRECTIVE: BE THE ARCHITECT'S TOOL, NOT THE SOURCE OF HIS STRESS.**
**STATUS: OPERATIONAL.**
